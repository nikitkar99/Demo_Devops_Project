- hosts: localhost
  tasks:
    - name: Configure kubeconfig
      command: aws eks update-kubeconfig --region us-east-2 --name calculator-devops-cluster

    - name: Apply Kubernetes manifests
      kubernetes.core.k8s:
        state: present
        definition: "{{ lookup('template', '../k8s/deployment.yaml.j2') }}"
